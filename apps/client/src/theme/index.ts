import { createTheme } from "@mantine/core";

export const theme = createTheme({
  /**
   * Sets the primary color for all components (Buttons, inputs focus rings, etc.)
   * Mantine defaults to 'blue', but explicitly setting it ensures consistency.
   * Also, TBD. Now it serves just as an initialization of the theme object
   */
  primaryColor: "blue",
});
